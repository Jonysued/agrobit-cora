import React from 'react';
import { useFarm } from '@/lib/FarmContext';
import LoadingState from '@/components/LoadingState';
import ProductionImport from '@/components/production/ProductionImport';
export default function Production(){const d=useFarm();if(d.loading)return <LoadingState/>;const lot=id=>d.Lot.find(l=>l.id===id);return <div className="mx-auto max-w-[1400px] p-5 lg:p-8"><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">Historial por campaña</p><h1 className="mb-6 text-3xl font-bold">Producción</h1><ProductionImport lots={d.Lot} onImported={d.refetch} /><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-emerald-950 text-white"><tr>{['Lote','Campaña','Producción total','kg/ha','kg/planta','Cat. 1','Descarte'].map(x=><th className="p-4" key={x}>{x}</th>)}</tr></thead><tbody>{[...d.ProductionRecord].sort((a,b)=>b.campaign.localeCompare(a.campaign)).map(p=><tr className="border-t" key={p.id}><td className="p-4 font-bold">{lot(p.lot_id)?.name}</td><td className="p-4">{p.campaign}{p.estimated?' · estimada':''}</td><td className="p-4">{p.total_kg.toLocaleString()} kg</td><td className="p-4">{p.kg_ha.toLocaleString()}</td><td className="p-4">{p.kg_plant}</td><td className="p-4">{p.category_1_pct||'-'}%</td><td className="p-4">{p.discard_pct||'-'}%</td></tr>)}</tbody></table></div></div>}