import React,{useState} from 'react';
import { Upload } from 'lucide-react';
import { base44 } from '@/api/base44Client';
import { useFarm } from '@/lib/FarmContext';
import LoadingState from '@/components/LoadingState';
const types={lotes:'Lot',produccion:'ProductionRecord',objetivos:'Objective',sanidad:'HealthRecord',riego:'IrrigationDesign'};
export default function Settings(){const d=useFarm(),[type,setType]=useState('lotes'),[busy,setBusy]=useState(false),[ranges,setRanges]=useState(()=>JSON.parse(localStorage.getItem('mapRanges')||'{"low":30000,"high":38000}'));if(d.loading)return <LoadingState/>;const load=async e=>{const f=e.target.files[0];if(!f)return;setBusy(true);const text=await f.text(),[head,...lines]=text.trim().split(/\r?\n/),keys=head.split(',').map(x=>x.trim());const rows=lines.map(line=>Object.fromEntries(line.split(',').map((v,i)=>[keys[i],v.trim()])));await base44.entities[types[type]].bulkCreate(rows);await d.refetch();setBusy(false)};return <div className="mx-auto max-w-[1000px] p-5 lg:p-8"><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">Administración</p><h1 className="mb-6 text-3xl font-bold">Configuración y cargas</h1><section className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Importación masiva CSV</h2><p className="mt-1 text-sm text-slate-500">Cargá maestros e históricos sin ingresar registros uno por uno.</p><div className="mt-5 flex flex-wrap gap-3"><select value={type} onChange={e=>setType(e.target.value)} className="rounded-xl border px-4 py-3">{Object.keys(types).map(x=><option key={x} value={x}>{x[0].toUpperCase()+x.slice(1)}</option>)}</select><label className="flex cursor-pointer items-center gap-2 rounded-xl bg-emerald-900 px-4 py-3 font-bold text-white"><Upload size={17}/>{busy?'Importando…':'Elegir CSV'}<input type="file" accept=".csv" onChange={load} className="hidden"/></label></div></section><section className="mt-5 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Rangos del mapa productivo</h2><p className="mt-1 text-sm text-slate-500">Definí los cortes de desempeño en kg/ha.</p><div className="mt-4 flex flex-wrap items-end gap-3"><label className="grid gap-1 text-sm font-bold">Límite rojo<input type="number" value={ranges.low} onChange={e=>setRanges({...ranges,low:Number(e.target.value)})} className="rounded-xl border px-3 py-2"/></label><label className="grid gap-1 text-sm font-bold">Inicio verde<input type="number" value={ranges.high} onChange={e=>setRanges({...ranges,high:Number(e.target.value)})} className="rounded-xl border px-3 py-2"/></label><button onClick={()=>localStorage.setItem('mapRanges',JSON.stringify(ranges))} className="rounded-xl bg-emerald-900 px-4 py-2 font-bold text-white">Guardar rangos</button></div></section><section className="mt-5 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Campañas</h2><div className="mt-4 flex flex-wrap gap-2">{d.Campaign.map(c=><span key={c.id} className={`rounded-full px-4 py-2 text-sm font-bold ${c.is_current?'bg-emerald-900 text-white':'bg-slate-100'}`}>{c.name}{c.is_current?' · actual':''}</span>)}</div></section></div>}