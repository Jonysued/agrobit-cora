import React from 'react';
import { useFarm } from '@/lib/FarmContext';
import LoadingState from '@/components/LoadingState';
export default function Health(){const d=useFarm();if(d.loading)return <LoadingState/>;const lot=id=>d.Lot.find(l=>l.id===id);return <div className="mx-auto max-w-[1300px] p-5 lg:p-8"><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">Últimas campañas</p><h1 className="mb-6 text-3xl font-bold">Historial sanitario</h1><div className="grid gap-3">{d.HealthRecord.map(h=><article key={h.id} className="grid gap-3 rounded-2xl border bg-white p-5 shadow-sm md:grid-cols-[120px_1fr_140px]"><div><b className="text-emerald-800">{lot(h.lot_id)?.name}</b><p className="text-xs text-slate-500">{h.campaign}</p></div><div><h2 className="font-bold">{h.problem}</h2><p className="text-sm text-slate-500">{h.notes||h.result||'Sin observaciones adicionales'}</p></div><span className={`h-fit rounded-full px-3 py-1 text-center text-xs font-bold ${h.incidence==='Alta'?'bg-red-100 text-red-800':h.incidence==='Media'?'bg-amber-100 text-amber-800':'bg-emerald-100 text-emerald-800'}`}>Incidencia {h.incidence}</span></article>)}</div></div>}