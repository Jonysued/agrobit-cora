import React from 'react';
import { ResponsiveContainer, ComposedChart, Bar, Line, XAxis, YAxis, Tooltip, CartesianGrid, Legend } from 'recharts';
export default function ProductivityChart({history=[],objective}){const data=history.slice(-5).map(x=>({campaña:x.campaign,'kg/ha':Math.round(x.kg_ha),Objetivo:objective?.kg_ha}));return <div className="h-72 w-full"><ResponsiveContainer><ComposedChart data={data}><CartesianGrid strokeDasharray="3 3" stroke="#dfe7dc"/><XAxis dataKey="campaña" tick={{fontSize:12}}/><YAxis tickFormatter={v=>`${v/1000}t`}/><Tooltip formatter={v=>`${Number(v).toLocaleString('es-AR')} kg/ha`}/><Legend/><Bar dataKey="kg/ha" fill="#166534" radius={[6,6,0,0]}/><Line dataKey="Objetivo" stroke="#d97706" strokeWidth={3} dot={false}/></ComposedChart></ResponsiveContainer></div>}