import React from 'react';
export default function HealthTab({lot,data}){const rows=data.HealthRecord.filter(x=>x.lot_id===lot.id),counts=rows.reduce((a,x)=>(a[x.problem]=(a[x.problem]||0)+1,a),{});return <div className="grid gap-5 lg:grid-cols-[1fr_2fr]"><section className="rounded-2xl bg-emerald-950 p-6 text-white"><h2 className="text-lg font-bold">Problemas recurrentes</h2>{Object.entries(counts).sort((a,b)=>b[1]-a[1]).map(([p,n])=><p key={p} className="mt-4 rounded-xl bg-white/10 p-4 text-sm">Este lote presentó <b>{p}</b> en {n} {n===1?'campaña':'campañas'}.</p>)}</section><section className="space-y-3">{rows.map(h=><article key={h.id} className="rounded-2xl border bg-white p-5"><div className="flex justify-between"><div><b>{h.problem}</b><p className="text-xs text-slate-500">{h.campaign} · {h.date}</p></div><span className="text-sm font-bold">Incidencia {h.incidence}</span></div><p className="mt-3 text-sm text-slate-600">{h.notes||h.result||'Sin observaciones'}</p></article>)}</section></div>}